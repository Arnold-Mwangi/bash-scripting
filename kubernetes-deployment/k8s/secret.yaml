# Demo credential — replace before production use.
apiVersion: v1
kind: Secret
metadata:
  name: demo-app-secret
  labels:
    app: demo-app
type: Opaque
stringData:
  API_SECRET: "change-me-in-production"
