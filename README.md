# bash-scripting

Developed and tested on **Arch Linux** (systemd, `iproute2`; OS info from `/etc/os-release` when `lsb_release` is absent).

System health check script: `system_health.sh` (CPU, memory, disk, network, services; logs to `/var/log/system_health.log`).
